# CARRY.md — quickserve-ai/gascity carry model (one fleet build)

This file records what is specific to this fork: the one build it runs, its branches, the
behaviors it carries above upstream main, the beads pin, and how the carry is re-synced and
landed. The gas-city-carry skill governs procedure. Git governs state: when this file and Git
disagree, Git wins.

The ledger, dropped and count sections are generated by `gen_carry_md.py` in resync-tools, from
the re-sync's decisions and its linearize report. To change a row, change its inputs and
regenerate; do not edit the tables by hand.

## One build

Overseer ruling, 2026-09-15 07:36Z:

- Quickserve AI runs one `carry/operational` build, on westeros and on both laptops.
- Every machine runs the current fleet unit within 24 h of its attestation, or within 72 h over a
  weekend.
- Keep or drop is decided once per behavior, for the whole fleet. The ledger has no town split.
- The fork owners are navani (Alex town) and woodhouse (Cherub town). They decide rows jointly.

## Branch state

| Ref | Contract | At the 2026-09-17 re-sync |
|---|---|---|
| `main` | Mirrors `gastownhall/gascity` main. No carry patches. | Refreshed at each re-sync with `git push origin upstream/main:main`, an admin push (the rig-ref-confinement ruleset covers `main`). |
| `carry/operational` | The deploy lineage and the fork's default branch. Fleet units are tagged on it. | Sits on upstream main `ad34febe7` (2026-09-17). It folds the pre-rewrite carry through `d60e1399d` (fork #74); anything merged to the carry after that commit is not in it. List the stack with `git log --oneline "$(git merge-base upstream/main origin/carry/operational)"..origin/carry/operational`. |
| `archive/*` tags | Rollback pins for rewritten tips. Tag and push the current tip before every rewrite. | `archive/carry-operational-pre-20260917` pins the tip before this re-sync, so every commit that was on `carry/operational` resolves under it; `archive/carry-operational-pre-20260915` still pins the tip before the 2026-09-15 rewrite. The dropped rows name ORIGINAL carry SHAs from the pre-09-10 lineage, which the 2026-09-17 tag does not reach: those resolve under `fleet/2026-09-10` (`7b7ce4db2`), an ancestor of `archive/carry-operational-pre-20260915`, so under that tag too: 75 of 75 measured (woodhouse, 2026-09-15), 76 of 76 re-measured at the 2026-09-17 re-sync. |
| `fleet/YYYY-MM-DD` tags | One fleet unit each, per qc-bridge `shared/fleet-gc-binary-roll.v1.md`. | Units cut so far: `fleet/2026-09-15`, `fleet/2026-09-17`, `fleet/2026-09-17.1` and `fleet/2026-09-17.2`; none has been moved. |

Remotes are verified with `git remote -v`, never assumed. Here `origin` is `quickserve-ai/gascity`,
the shared mirror that carries `carry/operational`, and `upstream` is `gastownhall/gascity`.
Upstream contribution branches start from fetched `upstream/main` and go to the contributor's
personal fork, with maintainer edits allowed, never to `origin`.

## The ledger

Every kept row is an intentional divergence from upstream main. A re-sync that loses one silently
is a regression. "Drop when" is the absorption test: before dropping a row, prove it by patch-id or
range-diff and by the named behavior check on upstream.

Commit cells name commits on `carry/operational` after the 2026-09-17 re-sync. "(replayed)" marks a
commit taken from an upstream PR branch; it drops by patch-id when that PR merges. "folded into"
names the later commits that carry a row whose own re-expressed commit went empty at linearize;
"(integration)" marks the integration commit. A commit added after integration carries its note in
parentheses. The commit that adds this file sits on top of the stack and is not listed.

### Session liveness and beads cache (4)

| Behavior | Commits | What / why | Upstream | Drop when |
|---|---|---|---|---|
| Session liveness off Dolt history (ga-lys454) | 30d2ac26e, a9b1e087c, c42c19987 (private liveness handle opened through doltpool.OpenPrivate), de2f97202 (liveness plan maps sized without length arithmetic (CodeQL allocation-size-overflow)) | Session-bead liveness writes go to a dolt_ignored `session_liveness` table and reads overlay it, so a liveness change mints no Dolt commit (about 250 an hour before). Upstream #5235 covers new session rows only, and a binary without the overlay would read stale values that live only in the table; `suspended_at` and `wake_refused_event_at` joined the moved keys at the re-sync (Q1). | #5235 covers new session rows; the overlay is carry-only | No open session bead is left in the versioned issues table on hq and qcore (`select count(*) from issues where issue_type='session' and status<>'closed'` is 0 after `bd update --no-history`), `session_liveness` is flushed into bead metadata, and a binary without the overlay measures near-zero session commits per hour. |
| gc hook --claim declines a bead whose recorded session is still live (ga-pzop1c) | 144df486e | A claim skips a bead whose `gc.session_id` names a live session, and a takeover records the displaced id in `gc.prev_session_id`. Incident qc-53bmzg (2026-09-03): a reaper-cleared bead put two live sessions in one worktree for about 15 minutes. | none; carry-only (#5841, #6328 and #6265 narrow the trigger, not the claim) | Upstream `claimFirstEligibleHookCandidate` checks the recorded session's liveness before claiming; `cmd/gc/cmd_hook_claim_live_owner_test.go` passes on upstream. |
| Beads cache reconciler stall fixes (ga-yc0chj) | 6770b8440, 93d2d7ec6, fdd3fb9b3 | A hung initial prime no longer leaves a store without its reconciler (5-minute arm bound), the first full scan is anchored to arm time instead of the last write, and a cache that stopped reconciling refuses List, Count and Ready instead of serving stale rows (CachedList stays unbounded). Incidents: the city cache blind for 3h31m on 2026-08-13, and a 43h stall with order-tracking sweeps reading zero rows (ga-v5vnyp). | none; carry-only (#5611 removes one churn source, not the anchor) | Upstream arms the reconciler despite a hung prime, anchors the first scan to arm time and bounds cache freshness on the serving gates; `cmd/gc/api_state_prime_bound_test.go` and `internal/beads/caching_store_first_reconcile_starvation_test.go` pass on upstream. |
| Beads cache reconcile observability (ga-yc0chj) | cf61f7b88, 3b2e242e9, eafc6512b | A durable per-scope heartbeat record, the `gc doctor` beads-cache-reconcile check and an in-process overdue watchdog detect a reconciler that stopped. Upstream ages cache health from the last write, so a stalled reconciler reads healthy. | none; carry-only | Upstream detects a stalled cache reconciler with a signal local writes do not reset, and its doctor reports it; drop the record, the check and the watchdog together. |

### Notifications, nudges, mail, prompts, providers (21)

| Behavior | Commits | What / why | Upstream | Drop when |
|---|---|---|---|---|
| Prompt fragments: raw files, pack-qualified aliases, fail-loud (ga-14a, ga-80ij) | f0a51d88f, 54644f169 | Define-less fragment files resolve as fragments, fragments get `<pack>/<fragment>` aliases, and `gc prime --strict` fails on a missing fragment or an unknown template variable. Upstream parses a define-less file into the root body, so a missing fragment renders silently. | none; carry-only (#6322 fixes a different silent path) | Upstream resolves raw fragment files and strict prime reports missing fragments and unknown variables; the fragment cases in `cmd/gc/prompt_test.go` pass on upstream. |
| OMP seat correctness: hook v3 PATH, v4 prime injection, lineage fix (ga-vat7sn) | 06551ef05, b6758f7a4 | The omp hook prefers the managed gc on PATH (v3) and injects prime output into the session (v4), and a provider re-point corrects a stale `provider_kind` and `builtin_ancestor`. Without v4 a primed or compacted omp seat never sees its role context (ga-c0v33s). | none; upstream's omp hook is still v2 (#5342 did this for pi only) | Upstream's omp hook injects prime output with #5342's hook markers and finds gc through `GC_BIN`, and a re-point refreshes lineage; the omp cases in `internal/hooks/hooks_test.go` pass on upstream. |
| Mail durability: 24h handoff TTL, mark-unread reopens (ga-1kor) | 763f92488 | Handoff mail gets a 24-hour sweep TTL instead of the 60-minute read-mail sweep, and `mark-unread` reopens a closed message. Upstream sweeps all read mail on one cutoff, and no config knob sets a handoff TTL. | #5805 withdrawn 2026-09-01; carry-only | Upstream sweeps handoff mail on its own cutoff and `MarkUnread` reopens closed messages; `internal/mail/beadmail/beadmail_retention_test.go` passes on upstream. |
| Navigator classification fields on session list JSON and the session API | 2f012433d | `gc session list --json` and the session API carry `session_origin`, `pool_managed`, `control_plane`, `base_state` and `navigator_schema_version`. qc-bridge operator-ui session-nav reads them (`checks/session-nav.sh`, `session-nav-list.sh`). | #4582 closed unmerged; carry-only | Upstream exposes the same wire fields, or session-nav stops reading them. |
| Context-window guidance: launch-model stamping, unknown-model warning (ga-pipu6) | b9c389f92, 2dc708242 | gc stamps `GC_CONTEXT_LAUNCH_MODEL` into session env and warns once per model that has no known context window. Upstream's modelwindow already classifies every current Claude id, so this matters only for future ids; testenv now scrubs the variable. | #4527 absorbed the window table; stamping is carry-only | Fleet 1M seats set `context_advisory.window_tokens` in config (upstream #5464), or upstream stamps the launch model; the launch-model cases in `cmd/gc/context_inject_test.go` then go. |
| Claude model enum: `opus` and `fable-5` at [1m] | 715e4f63d, d0ad0e34a, d7a8c0237 (launch-path test: opus launches as opus[1m] on upstream #5658's open model option) | Builtin claude choices map `opus` to `opus[1m]`, `opus-5` to `claude-opus-5[1m]` and `fable-5` to `claude-fable-5[1m]`, with `opus-4-8` and `fable-5-200k` pins. Dropping it would silently move every provider on the builtin list to upstream's `claude-opus-4-8` (no 1M window) at its next start, including `claude-opus`, the alex city's default for machine and workflow roles; Cherub's city and westeros are unmeasured (Q5). | #4740 added canonical ids; #5658 made the model option open but still maps `opus` to `claude-opus-4-8` (no 1M window), so these aliases remain the only launch path to [1m] for a short value; carry-only | Each city pins its claude choices with `[providers.claude] options_schema_merge = "by_key"` (the resync-followup bead), measured on the alex city, Cherub's city and westeros; then take upstream's `profiles.go` and its `TestBuiltinClaudeModelChoicesIncludeOpus5`, which the carry edits with a divergence comment. |
| Sling routes a singleton template to its configured named session | 26021af13, 757e79643, c9ec0083a | `gc sling` to a singleton template targets its configured named session, so no unnamed nudge shadow is created beside it; `RoutedToIdentity` stays the idempotency fallback (ga-mneb98). Upstream routes to the template's qualified name. | none; carry-only | Upstream sling resolves a template that backs a named session to that session; `TestCmdSlingRawSingletonTemplateRoutesToCanonicalNamedSession` passes on upstream. |
| OMP startup readiness delay (ReadyDelayMs 8000) | db7eca43e | The builtin omp profile waits 8 seconds before the first nudge so the TUI is ready. Upstream's omp profile has no readiness signal; `[providers.omp] ready_delay_ms = 8000` in pack config expresses the same fix. | none; config knob available | Every fleet city sets `[providers.omp] ready_delay_ms` through the qc-bridge pack, or upstream's omp profile declares a readiness delay. |
| herdr agent-start timeout from the context deadline; startup_timeout 300s (ga-fcdvn) | 607f75cdd | herdr derives the agent-start `--timeout` from the start context instead of a fixed 60 seconds, and `[session] startup_timeout` defaults to 300s. Incident on the herdr runtime: 37 kill and restart cycles and a quarantined merge-queue consumer. | none; the 300s default is also a config knob | Upstream herdr derives the agent-start timeout from the context deadline; set `startup_timeout` in city config and take upstream's default (`TestValidateDurationsSetupTimeoutExceedsDefaultStartupTimeout` then needs no adaptation). |
| Cross-city `gc mail send`, `reply`, `inbox` and `mark-read` via --context (#5386) | 2c3b02237, 63d0adbbd, 41bca1bc4 (fork #74: remote mark-read over --context, dispatched like send and reply), 0f9245526 (fork #74: honest help text; '.' and '..' ids refused), 874411024 (fork #74: one loopback-server construction site, census unchanged) | `gc --context <peer> mail send`, `reply`, `inbox` and `mark-read` route to a remote city over the control plane; `mark-read` (fork #74, ga-9n0qvp) lets a cross-town poller acknowledge what it consumed, while `read`, `archive` and `count` stay refused remotely. Upstream still refuses remote contexts for mail. | PR #5386 (aphexcx, open) covers send, reply and inbox; remote mark-read is carry-only | #5386 merges and upstream grows a remote mark-read (until then keep the mark-read arm); take upstream's `cmd/gc/mail_remote.go` (its head adds a grant-gated listener this pick lacks). |
| Notification plane and claude-cloud wake transport (ga-bjbaui) | 1963d43d3, e4c41705e, ca084faef, 0dcb0f2e3 (cloud-wake-bindings counted among the store-gated doctor checks) | Typed wake events above the provider boundary carry the mail bead id, `wake_transport` selects a transport per seat, and the `claude -p --cloud` transport comes with `gc session bind-cloud`, `mail --ref` and doctor facts. Upstream has only the message-id plumbing (#5364); mail wakes no longer carry `Summary` (nothing read it). | none; carry-only | Upstream ships a per-seat wake transport and a cloud wake path, or the fleet retires claude-cloud seats. |
| Mail notify-on by default for direct local sends (ga-bjbaui) | 3e40ab5dc | `gc mail send` to a local recipient requests a wake unless `--no-notify` is given. Ratified 2026-09-03 after a 67-minute unread-deadline near miss; upstream defaults `--notify` to false. | none; carry-only | Upstream defaults to notify, or every sending prompt passes `--notify`; then revert the `--no-notify` pin in `cmd/gc/testdata/gastown-events.txtar`. |
| Codex and OMP model choices: `gpt-6-astra`, omp model and thinking | 9ab087569 | Adds `gpt-6-astra` to the codex choices and an OptionsSchema for omp (model pins with an effort suffix, thinking level). After upstream #5658 made model and effort open options, an unlisted codex model validates and launches verbatim, so the astra entry is only a picker label. The omp schema is still carry-only, and its model option is now open (`FlagTemplate`), as #5658's `TestOpenModelPinIsHonoredForEveryProvider` requires. | #5658 absorbed codex validation; the omp schema is carry-only | The qc-bridge pack carries the omp choices as by_key provider overrides, or upstream ships an omp schema; the astra picker entry may be dropped at any time. |
| Queued nudges carry self-reported sender provenance (ga-txbsqo) | a150fd581 | Queued nudges record the sender and sender session, and inject output shows them. A trace aid, not authority. | none; carry-only | Upstream's nudge queue records sender provenance, or the fleet stops reading it. |
| Claude launches carry `--name <seat identity>` (ga-n0rvsk) | 4b26d8829, 952b93220 | Claude launch and resume commands, and the controller spawn path, append `--name` with the seat's addressable identity, so `/resume` rows and messaging name the agent. The name is appended after the fingerprint stamps, so adding it does not restart the fleet (ga-2pcujo). | none; carry-only | Upstream names claude sessions at launch; `cmd/gc/session_name_spawn_test.go` passes on upstream. |
| Operator attention registry: omp v5 hook, who's-waiting pane (ga-s0fn27) | ef023cf31 | The omp v5 hook writes seat attention events to a registry, and the dashboard's who's-waiting pane shows them with confirmed, unconfirmed or unknown evidence. Upstream #5417 shows bead-level cues only. | none; carry-only | Upstream's dashboard shows seat-level waiting-on-human state from hook events, or the fleet retires the pane. |
| Nudge state.json keeps newer-schema fields; pollers re-exec on a binary swap (ga-aj9auz) | 2fb6c54f3 | An older gc rewriting `state.json` keeps fields it does not know, and a nudge poller re-execs when its binary is replaced. Upstream round-trips the file through its struct, so a mixed-version window strips newer fields. | none; carry-only | Upstream preserves unknown state fields and restarts stale pollers; `internal/nudgequeue/schema_roundtrip_test.go` and `cmd/gc/nudge_poller_stale_test.go` pass on upstream. |
| gc prime says why it rendered nothing (ga-vuh3tj) | 56dfbc08b | A prime that renders nothing says why on stderr (a suspended seat, a resolution miss); hook mode stays silent. Upstream returns empty output with no warning. | none; carry-only (#6322 covers a different silent path) | Upstream prime warns on suspended-only resolution and resolution misses; the new cases in `cmd/gc/cmd_prime_test.go` pass on upstream. |
| renudge-stale-human-gates: 600s timeout; a failed prune keeps state (ga-2essre) | 1a00d47cf | The order timeout is 600s instead of 120s, and a failed prune no longer blanks the persisted dedup state. Upstream #4771 already persists state per send, so the carry's second `write_state` was dropped at the re-sync. | #4771 absorbed per-send persistence; the rest is carry-only | Upstream's script guards the prune (`PRUNED`), and the timeout moves to an `[[orders.overrides]]` entry in city config. |
| Cross-town mail send refusal names the hub leg (ga-5toacq) | 13701eec9 (fork #69: the local and hub refusals name the hub leg), 0f40467f9 (fork #69: no pack role named; one command per context), 7e9ccdcc1 (fork #69: agent dirs count as local; silent without config) | A local `gc mail send` to a recipient whose prefix names no rig, agent directory or city of this one is refused with a hint naming the hub leg instead of a bare 'session not found': one copyable `gc mail send --context <name> ...` per context the client holds, or a `<hub context>` placeholder when it holds none. Agent directories count as local, and the hint stays silent when the city config did not load. It changes only the text of a send that already failed. | none; carry-only (#5386 covers remote routing, not the local refusal text) | Upstream's local refusal names remote contexts, or the fleet stops addressing hub mail from local sends. |
| Mail reads wait 25s on the server and 30s on the client (ga-x49mfh) | 007737f0b (fork #70: server mail read deadline 25s, client mail reads 30s) | The API's mail read deadline (list, get, count, thread) is 25s instead of 8s, and the client's mail reads carry their own 30s deadline, so a slow hub store still answers with a typed store_slow before the client gives up. Other client calls keep the 60s ceiling. Incident: the hub's mail list query measured 7.4-8.4s, so the 8s deadline failed about 92% of cross-town mailbox reads on 2026-09-15/16. | none yet; an upstream candidate (upstream's 8s dates from a 10s client ceiling that is now 60s) | Upstream's server mail read deadline covers the hub's measured list latency and stays below the client's mail deadline, or the hub's mail list query runs under 8s. |

### Reconciler, sessions, pools, reapers (28)

| Behavior | Commits | What / why | Upstream | Drop when |
|---|---|---|---|---|
| Stale-db dog terminal close carries the claim identity and fails loud (we-m34w5) | 3f4993934, 91d6099ec (replayed) | `mol-dog-stale-db` closes its work bead presenting the claim identity in the writer's order (`GC_ALIAS`, `GC_SESSION_ID`, `BEADS_ACTOR`, `GC_AGENT`, `GC_SESSION_NAME`), and a refused close escalates and leaves the bead open instead of draining clean. Upstream (#6375) passes `--actor` with the session name ahead of the session id, and its close is unguarded. Re-sync #2: the resolution adopted upstream's packlint guard-ack comment and the wording 'terminal close refused'. | #6375 (merged 2026-09-16) added `--actor` to the close, with the session name ahead of the session id; it has neither the fail-loud guard nor the claim writer's identity order (`hookClaimAssigneeIdentity`: `GC_ALIAS`, `GC_SESSION_ID`, `BEADS_ACTOR`, `GC_AGENT`, `GC_SESSION_NAME`). Draft #5818 needs a rescope against it (gc-qyz5). | Upstream's terminal close is guarded (fails loud and escalates) and presents the claim identity in the writer's order. |
| Reconciler keeps a live pool seat with assigned work out of timer drains (gc-mdh0) | 95fdd4357 (replayed) | A live pool seat holding open or in_progress assigned work is not drained by the reconciler's own timers; explicit intents still drain. It stopped the review plane's mid-molecule drains, sixteen in one day. | PR #6168 (open); replayed from b3bd73102 | #6168 merges; the replayed commit drops by patch-id. |
| Heartbeat-held pool seat stays out of the orphan drain; reconciler ack ownership (gc-hzj5) | 787e2ab4f, 9fbb7bc9c, 6280ffb89, 431430e16 (replayed) | A seat with a live heartbeat hold is not drained as orphaned, and the reconciler publishes its drain ack under its own keys, so the hold's cancel never erases an agent's ack. It stopped the second review-plane drain class. | PR #6178 (open, issue #6173); replayed from its head b6bcb0094 | #6178 merges; the four replayed commits drop by patch-id. |
| Standing sleep intent survives drain completion (#5561, fork PR #59) | 96a30eb4b (replayed), 3e94c4882 (fork #59 review, item 1: standing-hold decisions defer on a degraded liveness read), 207a31e90 (fork #59 review, item 2: a wait-hold reason left behind by a degraded release heals on the next real read) | `gc session suspend` (user-hold) and `gc session wait --sleep` (wait-hold) park a seat under a standing intent that now survives the drain it provokes: the completion re-stamps the intent and records it as the sleep reason, so the crash-recovery override no longer respawns a suspended seat that holds work, and the pool-slot gate no longer reaps a parked one; hold expiry releases user-hold together with `held_until`. The two follow-ups are carry-only, because the liveness overlay is fail-open: standing-hold decisions defer on a read marked `gc.liveness_read_degraded`, and a Phase-0 heal clears a wait-hold reason that a degraded release stranded. Open, and named rather than gestured at: the marker is consulted at exactly five sites (the drain-ack finalize, the drain-advance loop, the Phase-0 timer and reason heals, the wait-hold reason heal itself, and the heartbeat crash override); every other arm acts on liveness fields that a degraded read serves empty — ordinary wake/respawn and drain cancellation, the pool-seat gates (slot-freeable, one-shot reuse, demand floor), the two state heals that write `state` and `sleep_reason`, `clearSessionWaitHold`, and `PruneDetailed`'s bead close. The wake engine cannot consult it at all: `compute_awake_bridge.go` does not carry `LivenessReadDegraded` onto `AwakeSessionBead`. The inventory with file:line, and the marker-production hole in the batched list path, are on gc-ax93. | PR #6335 (open, issue #5561); replayed from fb035e6a6 with fork-only test adaptations; the degraded-read guards and the reason heal are carry-only | #6335 merges (the pick adapts three fork-only test files, so prove the drop by range-diff, not patch-id); the two follow-ups drop with the session-liveness row, whose fail-open read they guard. |
| Reconciler prune re-probe and fresh-wake drain cooldown (ga-1xiv, ga-kaei) | 2982e4d3e, 8350ca3a7 (fork #59 review, item 3: the cooldown extends a hold and never shortens one) | An agent-home prune re-probes the runtime first, and unknown liveness vetoes it (#5544); a drain-acked always+fresh named heartbeat gets a 5-minute `held_until`, which never shortens a longer hold (before 8350ca3a7 it overwrote a suspend's indefinite sentinel, and the seat respawned five minutes later). Incidents: a qcore refinery worktree deleted during the 07-14 kickstart re-adoption (ga-1xiv), and a self-draining heartbeat minting about 1,570 fresh transcripts a day (ga-kaei). | #6307 covers the named-home call sites; the rest is carry-only | Upstream re-probes liveness before pruning and cools down a self-draining named heartbeat; `UnknownRuntimeLivenessNeverPruned` in `cmd/gc/session_worktree_prune_test.go` passes on upstream. |
| Gitignore-aware skill fingerprint hashing (ga-rpf2) | 581fc4024 | Skill fingerprints skip gitignored paths and `.DS_Store`, so build artifacts inside a skill directory do not trigger config drift. Upstream skips only Python caches and editor backups. | none; carry-only | Upstream hashes skills honoring `.gitignore`; `internal/runtime/gitignore_test.go` passes on upstream. |
| Raw-template manual sessions do not shadow configured named sessions (ga-e4jb) | 40a62bbe8, 1f1685e06 | An alias-less `gc session new` refuses a single-session template that backs a distinct-name named session, template starts collapse onto the named session, and shadows are retired (the "two lanas" incident: a shadow shared the named session's work dir and came back after every kill). Upstream #3884 Fix B handles the same-name case, so the refusal applies only when Fix B finds no alias owner. | #3884 covers the same-name case; distinct names are carry-only | Upstream finds named sessions by backing template on create and start; the ga-e4jb cases in `cmd/gc/cmd_session_test.go` pass on upstream. |
| gc session history and resume, with prior_session_key (ga-oe8h) | 03e3b678b, 08a5f21d8, eee1e7bd8 | `gc session history` lists an agent's past conversations and `gc session resume` reopens one; every session-key clear or rotation stamps `prior_session_key`. Upstream has neither command. | none; carry-only | Upstream ships session history and resume with prior-key stamping on every rotation path. |
| Reconciler crash policy: lazy FPExtra drift, restart stagger, reset-marker self-heal (ga-9n5hj, ga-2aq43) | 0dfe74e8f, f4e461812, d26fad0d4 | Prompt-only (FPExtra) drift waits for the next conversation instead of restarting sessions, config-drift restarts are capped at two per tick, and a stale continuation-reset marker on a healthy session clears itself. Incident 2026-07-18: an FPExtra-only wave restarted the whole roster, and an armed marker hard-reset the attached mayor 3.5h later. | none; carry-only (lazy FPExtra differs from upstream's PROVISION class on purpose) | `cmd/gc/session_reconciler_crash_policy_test.go` passes on upstream unmodified. |
| gc bd assignee canonicalization (ga-i44k) | 8dcf65da9 | `gc bd` rewrites a hand-written assignee to the session's alias-first identity before bd runs. It fixed the manual-write half of the ~98-bead strand; upstream normalizes assignees only on API writes. | none; carry-only | Upstream canonicalizes assignees on the `gc bd` passthrough; `cmd/gc/bd_assignee_canonicalize_test.go` passes on upstream. |
| Drift-wave coordinator mail (ga-9n5hj) | 3367d24bc; part folded into df096d85f | `[session] drift_wave_notify` mails a configured coordinator once per config-drift wave, through the messaging store. It rides the crash-policy restart budget. | none; carry-only | Upstream notifies on drift waves, or the crash-policy row drops (this row goes with it). |
| Stopped named and namepool agent-home reaper; gc worktree-gc preview (ga-ijun) | 486ea44bd, 41d8a0e9a | Opt-in `daemon.auto_reap_stopped_agent_homes` reclaims safe homes of stopped named and namepool agents (fail-closed, no forced git removal), and `gc worktree-gc` previews it. Upstream reaps closed-bead worktrees only; the reap's rig-store enumeration carries `residency:allow` (Q2). Open: it reads session history and active-session refreshes from the work store, so reclamation stops once a city relocates sessions (gc-6e7t; fix before any city does). | none; carry-only (#5193 manages per-bead workspaces) | Upstream reclaims stopped named homes; `cmd/gc/agent_home_worktree_reaper_test.go` passes on upstream. |
| A pool worker's alias is an assignment identity (ga-rht4v5) | 6437d3f24, 10a1fc89f (transient pool-slot aliases excluded from assignment identities and the awake set (upstream #5241)) | The awake set and the drain and close guards match work assigned to a pool seat's alias (`AwakeSessionBead.Alias` moved here from the dropped ga-1rmn3a commit). Incident 2026-08-13: qcore/gastown.furiosa ran 12 spawn, claim and die cycles with no work done, because its own claim was invisible. | none; carry-only | Upstream `sessionAssigneeMatches` and the assignment identifiers include the alias; `cmd/gc/pool_alias_assignment_test.go` passes on upstream. |
| Agent ledger writes are never attributed to a human (ga-xs28em) | cd57b97fd, ec1d2aca4 (default actor is the session bead id before the pool slot label (upstream #5663)) | Inside a managed session, `gc bd` defaults `BEADS_ACTOR` to the alias-first identity when none is set and warns once on a mismatch. Incident 2026-08-17: the mayor wrote to the ledger as a human for about 12 hours after a restart. | #4981 covers session start env; the `gc bd` default is carry-only | Upstream's bd env defaults the actor from the session identity; the `applyAgentBdActor` cases in `cmd/gc/bd_env_test.go` pass on upstream. |
| CopyFiles-only drift accepted in place; handoff before a drift drain (hq-wi4ka) | df096d85f | Drift in CopyFiles alone is rebaselined in place instead of draining the session, and a drift drain first mails a handoff to the session. Incidents: two fleet-wide context losses, 2026-08-19 and 2026-08-20, both CopyFiles-only. | none; carry-only | Upstream accepts CopyFiles-only drift without a drain; the CopyFiles cases in `cmd/gc/session_reconciler_crash_policy_test.go` pass on upstream. |
| A crew restart keeps the agent's backlog (ga-sdynmb) | d7ddd90dc | Closing or restarting a configured named session no longer unassigns its open work. Incident: 91 crew beads detached across three roll windows. | none; carry-only | Upstream `unclaimWorkAssignedToRetiredSessionBead` skips configured named identities; `cmd/gc/session_beads_crew_roll_release_test.go` passes on upstream. |
| orphan-sweep.sh leaves a foreign `<rig>/<name>` claim alone | f8a937c8a, d39e05c26 (protected-identity summary printed before the escalation exit) | The core pack's orphan-sweep script skips an in_progress bead whose assignee is a well-formed identity this city does not configure. Incident: fifteen beads in the shared qcore store reset between 2026-08-21 and 2026-08-24. | none; carry-only (#5952 covers only the Go sweeper) | Upstream's `orphan-sweep.sh` protects claims it cannot observe. |
| Pool orphan sweeper leaves a foreign `<rig>/<name>` claim alone (#5952) | aa46f0fa3, 77304650c | The in-process orphan sweeper does not release a claim whose identity this city does not mint, at both release sites. Incident 2026-08-21: fifteen of another city's claims cleared in 34 seconds, and two of our P0s cleared the other way. | PR #5952 (ckumar1, open) | #5952 merges; `cmd/gc/pool_orphan_foreign_identity_test.go` passes on upstream. |
| Pool-seat claim declines foreign-instantiated molecules (ga-h4iqzr) | 6b4118793 | A pool-seat claim skips a molecule whose formula source lies outside this host's roots unless the formula declares `path_agnostic`, and reports `declined_foreign`. Incidents: duplicate cross-town execution on 2026-08-29 (qc-flbkc1) and 2026-08-30 (qc-gcj39p). | none; cross-town invariant agreed with westeros (R2a on qc-e8os51) | Upstream's claim path checks formula source roots; `cmd/gc/cmd_hook_claim_foreign_test.go` passes on upstream. |
| Closed-bead worktree reaper tick cost (ga-singc6) | f86aa4dca, 87c983b38, 3ad010e13 (reaper hands its session dirs to worktree discovery as upstream does (CI unparam; protection unchanged)) | The reaper runs the cheap local git gate first, gathers the host-wide liveness scan only when a candidate reaches it (the lsof fallback is bounded at 10s, under the reconciler tick), and memoizes pass-1 bead status for 5 minutes. Measured over 70h, the eager scan ran on 99.9% of ticks with no candidate and caused about 73% of tick inflation. | none; carry-only | Upstream defers liveness gathering and memoizes pass-1 reads; `cmd/gc/bead_worktree_reaper_lazy_liveness_test.go` and `cmd/gc/bead_worktree_status_cache_test.go` pass on upstream. |
| LiveFingerprint determinism test (ga-wd5se7) | 87c3e921d | Test only: pins that the live fingerprint hash is deterministic. It closed the ga-wd5se7 investigation and passes on upstream unchanged. | none; offer as a test PR | Upstream carries an equivalent test, or at any re-sync by owner choice (it guards no carried code). |
| Per-rig off switch for the in-process orphan sweeper (ga-h5435p) | 6bb2fca14, d156f8f84 (orphan close reads the owning rig from store refs; city-owned work keeps its exemption) | `orphan_release = false` on a rig holds both orphan release sites. The second site (`releaseConfirmedOrphanSessionWork`) is gated too, with its own test. | none; carry-only | Upstream adds a per-rig orphan-release knob; `cmd/gc/pool_orphan_release_gate_test.go` passes on upstream. |
| `gc agent is-foreign` verb (ga-7dr90m) | 2fa5587bb | Exposes the foreign-identity gate to shell callers with exit codes local, foreign and unknown. Incident 2026-08-26: the qcore witness patrol stripped 18 workflow steps held by westeros identities. | none; carry-only | The foreign-identity gate lands upstream with a CLI, or no formula or script calls the verb. |
| Bead-worktree git gate drops the repo-global stash veto (ga-gsfxag) | 7bf07367d | A stash no longer blocks reaping: `git stash list` is repository-global, so one stash protected every worktree of the repo, and `git worktree remove` cannot lose a stash. On the fleet it was 99.1% of reap skips, all clean pushed trees. | none; carry-only | Upstream removes the stash term from its git safety gate; `cmd/gc/bead_worktree_reaper_stash_gate_test.go` passes on upstream. |
| Bead-worktree git gate ignores gc provisioning sediment (ga-pi0rzc) | 873d68ba5 | Untracked `.claude/` and `.omp/`, anything under `.beads/` and the `.worktree-stale` marker no longer read as uncommitted work; before, 27 clean pushed trees held 90G until the 2026-09-05 manual sweep. Open: a trimmed first `git status` line can still block `.beads` sediment. | none; carry-only | Upstream's gate filters gc provisioning output; the sediment cases in `cmd/gc/bead_worktree_reaper_test.go` pass on upstream. |
| Claude account isolation: no ambient CLAUDE_CONFIG_DIR (ga-ai7gz2, ga-xd3bjx) | dbf676978 | A managed session never inherits the controller's `CLAUDE_CONFIG_DIR`: the passthrough resets it, a claude spawn with no declared account while the controller carries one is refused on create and warned on resume, respawn withholds the empty key, and a doctor check reads the supervisor's launchd plist. Incident 2026-09-09: two seats billed the wrong account. | none; carry-only (#5061 and #5421 absorbed the respawn and workspace-env items) | Every fleet seat declares `CLAUDE_CONFIG_DIR` at provider level and the reset is shown redundant, or upstream adopts an account-declaration guard. |
| Named-session conflict diagnostic and pool-shadow adoption (ga-dfp1b) | c7b28df0b, fc378f1bd (named mint fenced with the fresh session census; cross-family review finding A1), f5b2898bc (test helper agent made a constant (CI unparam)) | A skipped configured named session names the conflicting bead on stderr, and an adoptable pool shadow is adopted instead of wedging; the woodhouse and mallory wedges logged 4600+ silent skips. Upstream #5487's alias pass is accepted: a distinct-identity shadow converges in three ticks, and the conflict log still fires for a foreign-template holder or two matching holders (Q3). The create-side named mint re-reads the complete session census under the identifier locks and fails closed, like upstream's pool mint (found by the cross-family review at the re-sync). | #5487 covers the distinct-identity shadow; the rest is carry-only | Upstream logs the conflicting bead and adopts a pool shadow; `cmd/gc/named_session_adoption_test.go` passes on upstream. |
| Named seats: close fails closed without config, and a closing session never detaches work claimed under its durable identity (ga-9n8hjv) | f958e1e4a (folded from fork PR #64 at the 09-16 rebase), 634a727b8 (fork #68: a closing session keeps work claimed under its durable identity), 1c710aee4 (fork #68: withhold only for a configured-named session; pool release unchanged), 82e79a085 (fork #68: cfg tells a live named seat from a suspended one), 1e69ea324 (fork #68: US spelling in a test comment for the misspell linter), b71cc0e40 (re-sync #2: the test-only release wrapper keeps upstream's signature (lint-full unparam)) | A `gc session close` that could not load the city config released a named seat's whole portfolio, because the ga-sdynmb guard keeps work whose assignee is a still-configured [[named_session]] and a nil cfg matches nothing. The nil-cfg branch now releases only work bound to the session BEAD ID and says what it withheld. Incident 2026-09-11: 50 beads cleared in one write, 11 downgraded in_progress -> open, and the owner's own resume check then reported no work. Re-sync: folded from fork PR #64 after this stack was certified; the POOL-only rename of the Info form is not carried, because here that form already takes cityPath and cfg and sweeps through sweepAssignedWorkLegs, so the guard does apply to it. Re-sync #2: fork #68 (katya) folded at the 2026-09-17 re-sync. katya's decision: CARRY (ga-9n8hjv, 2026-09-17T08:43:12Z). The defect is still live at upstream 8f3b31751: `releasableAssigneeIdentities` is absent there, and upstream's release set includes the configured named identity. Upstream's preserve-set path (`closeBeadPreservingAssignees`) has one non-test caller, the dead-phantom recycle, which does not cover a canonical named seat closing its own bead. Keep both the cfg-aware live/suspended distinction and the conservative nil-cfg branch. | none; carry-only | Upstream withholds a live configured named seat's work when that seat closes its own bead (not only on phantom recycle), distinguishes suspended seats, and fails closed without config; cmd/gc session-close tests for all three pass on upstream. |

### Dolt packs, doctor, managed Dolt (15)

| Behavior | Commits | What / why | Upstream | Drop when |
|---|---|---|---|---|
| Dolt origin-mirror freshness plane | 712e80cda, af8a9c8ec | `gc dolt sync` and compaction stamp each successful or verified-current push, and `gc dolt health` reports mirror age under `backups.origin_mirrors` (unknown, stale or ok; worst case; 1800s default), refusing an empty classify count. Upstream #5329 dates local backups only, and its `dolt_*` fields keep that meaning. | #5329 covers local backups; mirrors are carry-only | Upstream's health measures off-box mirror freshness; the `origin_mirrors` cases in `examples/bd/dolt/health_test.go` pass on upstream. |
| gc dolt sync --dry-run exit code mirrors the real run (ga-co5cx) | efd0ef2f9 | A dry run returns non-zero for a diverged or classify-failed database, as the real run does. Upstream's dry run returns 0 for every database. | none; upstreamable | Upstream's dry-run skip branch returns the classify status; the dry-run cases in `examples/bd/dolt/sync_ffclassify_test.go` pass on upstream. |
| Doctor: a live rig Dolt DB on the managed endpoint is not an orphan (ga-w7xo) | db70b08ee | The noms-size scan recognizes a rig database served on the city's managed endpoint instead of reporting `orphan database`. Upstream's classification is unchanged since the old base. | none; upstreamable | Upstream classifies managed-endpoint rig databases as live; the ga-w7xo cases in `internal/doctor/checks_test.go` pass on upstream. |
| Doctor order-firing-current: never-fired orders are not green when the controller start predates the logs (ga-22tvtm) | 9973aa956, 76cced81a | With no controller start in the active log or any archive but firings present, a never-fired order reports an error (advisory for cron) instead of OK. The carry's 4-minute scan budget and its `CheckTimeout` clamp were dropped at the re-sync: upstream bounds the scan (#4790, #5203). | #4790 and #5203 absorbed the scan budget; issue candidate for the edge | Upstream `classifyOrderFiring` errors on a zero controller start with firings present; the edge cases in `internal/doctor/checks_order_firing_test.go` pass on upstream. |
| Doctor dolt-noms-size splits noms from git-remote-cache (ga-p8kao1) | 31a7c1272 | The check reports the noms store and the git-remote transport cache separately. Incident ga-npnoeo: a compaction acted on a figure that was about 60% cache, and hq lost off-box durability for about 121h. | none; upstreamable | Upstream's check separates the transport cache; the ga-p8kao1 cases in `internal/doctor/checks_test.go` pass on upstream. |
| Doctor dog dates local backups by sync success (ga-g3p5rm) | 878c02aec; part folded into fbb5702b8 | `mol-dog-backup.sh` stamps each successful sync, and `mol-dog-doctor.sh` alerts on the stamp's age. An up-to-date `dolt backup sync` does not rewrite the manifest, so manifest dating would page on every idle database; health keeps upstream #5329's manifest age as a report only, and the carry's `backups.local` plane was removed (Q8). | #5329 covers health; the doctor stamp is carry-only | Upstream's doctor uses a combined age (the stamp counts only while a manifest exists), upstream-first per the Q8 follow-up bead. |
| Managed Dolt remote credential read at spawn from dolt.remote-password-file (ga-10irxc) | 3e8336c4f, 93de89d4c | Managed Dolt reads `DOLT_REMOTE_PASSWORD` from the file `dolt.remote-password-file` names at spawn, instead of inheriting it. Incident: a watchdog started from an agent shell pinned an uncredentialed server, measured on two days running; it matters for cities that set the key (Cherub's hub sync). | none; the parser's early-return fix is upstreamable | Upstream sources remote credentials at spawn (`cmd/gc/dolt_remote_credential_test.go` passes on upstream), or no city sets `dolt.remote-password-file`. |
| Doctor provider-readiness guard; amp and auggie ready delays (ga-8ouxd) | 8c9bcc96a | Doctor flags a provider with no readiness signal, and the amp and auggie profiles get a ready delay. Upstream's amp, auggie and omp profiles declare none. | none; upstreamable | Upstream profiles declare readiness signals and doctor checks for them; `internal/doctor/checks_provider_readiness_test.go` passes on upstream. |
| gc dolt sync pushes an explicit remote list, with per-remote freshness (ga-3o5xrw) | fbb5702b8 | A comma-separated `GC_DOLT_REMOTE_<DB>`, such as `origin,probe`, pushes each listed remote with its own freshness stamp, and health lists parked mirrors. Upstream #6010 picks one remote and skips an all-https database without that variable, so a city that relied on pushing every remote sets `GC_DOLT_REMOTE_<DB>=a,b` on the dolt-remotes-patrol order env at its next bd/core re-pin. | #6010 makes remote choice deterministic; multi-remote push is carry-only | No fleet database needs two remotes pushed every run (each sets `GC_DOLT_REMOTE_<DB>` to one live remote), or upstream pushes an explicit list; `examples/bd/dolt/sync_multiremote_test.go` then goes. |
| Doctor StatusSkipped; the city store is checked for off-box remotes (ga-51iq0s) | c4b134e1e | A check that did not look reports `skipped`, not OK, and dolt-local-only-remote also inspects the city's own store; that gap let hq carry an off-box remote for 22h (ga-cc4wzn). `StatusSkipped` is -1, so upstream's warm-up ranking treats it as below OK without edits. | none; carry-only | Upstream's doctor has a skipped status and checks the city store; `internal/doctor/status_skipped_test.go` passes on upstream. |
| Doctor dolt-drift fails closed on an unresolvable port; dispatch panics log a stack (ga-4mkhyy) | 9b930e9e7 | The drift check no longer reports no drift when it cannot resolve the managed port, and a recovered order-dispatch panic logs its stack. Two small generic fixes. | none; upstreamable | Upstream fails the drift check without a resolved port and logs the panic stack; `cmd/gc/cmd_doctor_drift_test.go` passes on upstream. |
| Supervisor service env keeps BD_IGNORE_SCHEMA_SKEW and BD_DISABLE_METRICS (ga-v2yrs4, ga-ps5c4l) | folded into 35c585a31 | Both keys survive plist regeneration; upstream's `GC_SUPERVISOR_ENV` is read only from the invoking process, so a drift restart drops them. The skew key kept Cherub's controller alive through the v54/v59 skew window. | none (#6240 argues the same for BEADS_DOLT_POOL_*) | Upstream's `supervisorServiceEnvKeys` includes both, or no fleet machine sets them. |
| Doctor order-exec-target check (ga-swawpo) | 0732398ef | Flags exec orders whose script cannot launch; `--fix` restores the exec bit. | none; upstreamable | Upstream ships an exec-target check; `internal/doctor/checks_order_exec_target_test.go` passes on upstream. |
| Managed Dolt stop and exit attribution (ga-drkbcd, fork PR #54) | 8a1a2b3e2 | gc's own stops record stop-intent markers, the scope watchdog grades exits and signals by intent, and an unexplained stop raises an alarm. Observability added after the silent managed-Dolt stop of 2026-08-15; upstream logs only a clean or an error exit. | none; carry-only | Upstream attributes managed-Dolt stops, or the fleet stops running managed Dolt. |
| Carry-era canonical bundled pins listed as superseded (gc-970o) | e92875f8c (fork #71: the five carry-era pins listed, with doctor-fix tests) | `SupersededBundledPackImportVersions` also lists the five canonical core/bd/dolt pins this fork's lineage carried (f4128e404, 997b8a563, e37a541e1, abcf2b639b85, abcf2b6393a1). A city still pinned at one gets a not-cached error that names `gc doctor --fix`, and `doctor --fix --check packv2-import-state` re-pins to f895c0ff offline. Measured 2026-09-17: that rewrite re-encodes `pack.toml` and drops its comments (upstream `rewriteSupersededBundledPinsFS`). | none; carry-only (upstream never carried these shas) | No fleet city pins any of the five (westeros re-pinned in the unit-3 window; both laptops read clear) and the carry's canonical pin is upstream's. |

### Beads store, pins, Dolt endpoints (10)

| Behavior | Commits | What / why | Upstream | Drop when |
|---|---|---|---|---|
| Native Dolt metadata merge is a compare-and-swap (gc-sey7, #6222) | f79520e5c, 5fe28e8c3, 8aa02248e, d73b75589, db127691b (replayed) | `SetMetadataBatch` writes the merged map with the read's row version and re-runs read-merge-write on a version mismatch, so an update between read and write is merged, not overwritten. Incident: the lost update re-fenced review roots on westeros, 15 lost in 10 days. | PR #6233 (open); replayed from its branch ae5163dd3..25e86a128 | #6233 merges; the five replayed commits drop by patch-id. |
| Deterministic invalid-state port fallback test (release/v1.3.x pick) | 03d3cf8d3 | Test only: pins the managed port fallback when provider state is invalid. Authored upstream on release/v1.3.x and never merged to main. | release/v1.3.x only; offer to main | The test lands on upstream main, or at any re-sync by owner choice (it guards no carried code). |
| bd slow timer stops at command exit (ga-xcqw) | 37fb5c344 | The `bd.slow` timer stops right after the command returns, so a command that finished under the threshold cannot record as slow while telemetry runs. A four-line telemetry fix. | none; upstreamable | Upstream stops the timer before telemetry and classification; `internal/beads/bdstore_exec_internal_test.go` passes on upstream. |
| Refuse closing a bead whose recorded PR is confirmed unmerged (ga-sc80, qc-p45lz) | 5beb53789, 61a7b3bbb | `gc bd close` and dispatch's source-chain close refuse a bead whose PR GitHub confirms unmerged; missing gh, metadata or network fails open. Incident 2026-07-17: 9 of 12 bead-bearing open q-core PRs were orphaned by closes at submit. | none; fork workflow policy | A formula or pack merge gate covers the same closes, or the fleet retires the policy (`internal/prguard` goes with it). |
| Fail closed on native-store schema skew (ga-mw4dg) | a93d35ea0, c40a4b058 (pool liveness observed with the pre-reload provider, hooks after the skew hold), 2c79fa209 (a city.toml parse error is reported before a non-skew store preflight failure) | When a store's schema is newer than the binary, the controller holds session reconciliation, reloads and the boot sweep instead of silently falling back to the exec store, and preserves sessions without owning the city. Re-sync: re-expressed on upstream's `startOneCity` and `run()`; pool deaths run after the hold against the pre-reload handlers (R3a). | none; carry-only | Upstream holds or refuses on schema skew instead of degrading to BdStore; the schema-skew cases in `cmd/gc/city_runtime_test.go` pass on upstream. |
| Ambient Dolt identity bound to its endpoint (ga-3qvmjj, gc-49ho) | 52d591e5e, e7b54096f | An ambient `GC_DOLT_USER` or `GC_DOLT_PASSWORD` is declined on a provable host or port mismatch, in the doltauth resolver and on the direct-dial city-store paths; the pooled dial keeps an endpoint-bound password. Incident gc-49ho: fleet mail went deaf. | none; upstream pins the opposite contract; issue owed after the re-sync | Upstream binds ambient identity to its endpoint; `TestOrderRunExecDeclinesForeignEndpointIdentityForManagedLocal` and `TestOrderRunExecPreservesBareAuthOverridesForManagedLocal` pass on upstream. |
| Scope-aware beads-init endpoint (ga-tmhxnd, ga-uurd84, ga-298g8t, qc-ow3u50, hq-mbe2s) | 493848ecc, ff4b4b511 | A remote store keeps its own port, an inferred endpoint never overwrites a declared one, a derived origin warns instead of refusing start, rig init gets the scope credentials, and the post-init verify resolves the rig scope. Incident hq-mbe2s; at the re-sync the rig-scope check also covers upstream #4812's dirty-table recovery site. | none; issue owed after the re-sync | Upstream's beads init honors declared rig endpoints and scope credentials; `cmd/gc/dolt_config_explicit_endpoint_test.go` and `cmd/gc/dolt_compat_drift_inference_test.go` pass on upstream. |
| bd init dirty-schema checkpoint fails closed (release/v1.3.x pick) | folded into 493848ecc, ff4b4b511 | `gc-beads-bd.sh` checkpoints only allowlisted bd tables of a database this init created, per table with `--skip-empty`, and refuses otherwise. Upstream #4812's Go recovery commits with `DOLT_COMMIT('-A')`, so Go now honors the script's three refusals and commits at most once; before that guard the merge could commit operator rows the script refused (R5a). | #4812 took another route; issue owed on its `-A` (citing #5287) | Upstream's recovery refuses databases it did not create and stages only allowlisted tables; `TestGcBeadsBdInitDirtySchemaRecovery` passes on upstream. |
| Managed-local marker cleared explicitly across env overlays (fork PR #25) | ef580b6f9 | A managed target clears `GC_DOLT_MANAGED_LOCAL` explicitly, so a child env overlay cannot resurrect an inherited `0`; the endpoint-identity row reads that marker. Its explicit-empty `GC_DOLT_HOST`, `GC_DOLT_PORT` and `BEADS_DOLT_SERVER_HOST` projections were dropped at the re-sync as obsolete (#5154), and upstream's delete-not-blank tests restored. | #5154 fixed the hook-claim symptom; the marker clear is carry-only | Drops with the ambient Dolt identity row. |
| Beads fleet pin: gc links the fleet beads build through a go.mod replace (gc-1c2b) | 79072b6ff (replace guard admits the fleet tag, with fixtures), 7d176383e (the pinned-bd test helper builds from the replace target), ac4ad49d5 (the pinned integration bd honors the replace), d27d744ac (the go.mod replace, go.sum and the fleet replace test), 255b9a4cd (the contract bd-current cell builds from the replace target), 8d0d80178 (the module-graph guard admits the one sanctioned redirect), c374a8f33 (CI execution golden recomputed for both ci.yml edits), 6ab8372f1 (.3: migration 0062's temp tables born ignored (upstream beads #6031)), fa8fbe7c4 (the bd version stamp's purpose under a replace, stated) | `go.mod` keeps upstream's `github.com/steveyegge/beads v1.3.0-rc.2` require line and adds one replace to `github.com/quickserve-ai/beads` at the fleet tag, so gc's in-process store is the same beads code every machine's `bd` runs. Without it the fix for the 2026-09-14 review-ladder stall reaches the bd CLI only, and gc's own graph-apply and finalize closes keep the defect. The replace guard admits exactly the fork path at `vX.Y.Z[-rc.N]-fleet.<date>[.serial]`, the contract CI cell builds its bd from the module go.mod resolves, and `scripts/beads_fleet_replace_test.go` holds the replace's base in step with the require line. | none; the fleet's beads carries are fork-only | Upstream beads ships the fleet's carries in a release upstream gascity's `go.mod` requires, so the replace names the same code as the require line; then drop the replace, the guard admission and the fleet replace test. |

### Dispatch, orders, molecules, work queries (23)

| Behavior | Commits | What / why | Upstream | Drop when |
|---|---|---|---|---|
| Steps of a partially instantiated workflow are never dispatched (ga-033u0e) | ef5037867, d457b4547, d9fba95c7 (replayed) | Hook candidates, control-ready groups, demand counts and convergence re-activation skip the steps of a molecule marked `molecule_failed`. Upstream filters `gc.instantiating` only, so a failed mint made its hidden steps claimable: N steps in, N escalations out. | PR #5800 (open); replayed from its head 295dc0d36 | #5800 merges; the three replayed commits drop by patch-id. |
| Route recovery leaves fenced graph steps alone (gc-sey7, #6221) | ab3de2756 (replayed) | `carriedPoolRoute` yields no route for a bead behind the instantiation fence or of a ready-excluded type, so the backstop no longer stamps an unclaimable pool route onto a fenced entry step. Incident: 15 review roots lost in 10 days on westeros. | PR #6231 (open); replayed from its head 14d8fbb0c | #6231 merges; the replayed commit drops by patch-id. |
| City-imported rig-scoped orders fan out per rig (gc-8mb7, #6218) | 323674717 (replayed) | A city import's `scope = "rig"` orders expand once per rig with `Rig` stamped, instead of never firing. Kept this window: importing the pack at rig scope works without it, but moving westeros there is a city-config change. | PR #6232 (open); replayed from its head ee4b9bf74 | westeros imports the pack at rig scope (bead it with syl), or #6232 merges. |
| A failed mint stamps its own root and names what it could not confirm (gc-qum2, #6235) | 61323f3e4 (replayed) | `markFailed` retries a dropped `molecule_failed` stamp after the others, all 17 mark-then-return sites report unconfirmed stamps, and the dispatcher logs the error. Incident: roots with 7 of 8 and 6 of 7 members stamped stayed fenced and invisible on westeros. | issue #6235; replayed from branch f3457d6f7 (third-party PR #6270 overlaps, so we open none) | #6270 or an equivalent merges; drop once `internal/molecule/molecule_mark_failed_test.go` passes on upstream. |
| A native graph apply that exhausted its budget is not retried (#6333) | 6a6ba32ae, 005affeec (replayed) | A graph apply that ran out of its own budget returns `ErrGraphApplyBudgetExhausted` instead of being retried as a transient deadline. Incident: the westeros feeder's rc 124 kills; in unit 2, accepted by syl on the numbers. | PR #6334 (open); replayed from its head e37f1facf | #6334 merges; the two replayed commits drop by patch-id. |
| gc rig add materializes skills before it returns (#3652) | e1934d5bf | Skills exist when `gc rig add` returns, with no supervisor running and for out-of-tree rigs. Upstream merged this on release/v1.3.0 only; main materializes asynchronously on reload. | #3652 (release/v1.3.0 only) | Upstream main materializes skills in `cmdRigAdd`; `TestRigAddMaterializesSkillsIntoOutOfTreeRig` passes on upstream. |
| Per-rig default_merge_strategy (ga-frf3) | 8996a5aa0, aaec125cb, 3e22343f8 | A rig's `default_merge_strategy` writes `merge_strategy` metadata at dispatch, and `gc sling --merge pr` is accepted. | none; carry-only | Upstream adds a rig merge-strategy default; the merge-strategy list in `test/acceptance/sling_test.go` matches upstream's. |
| Fail closed on an out-of-vocabulary gc.failure_class (ga-033u0e) | f935c1d54 | An unknown failure class is hard, not transient, in retry and ralph. Upstream retries an unknown class. | none; upstreamable | Upstream treats an unrecognized class as hard; the ga-033u0e cases in `internal/dispatch/retry_test.go` pass on upstream. |
| Always+fresh advisory off ordinary commands; an empty gc hook says why (ga-3upjic) | a3fa03e84 | The always+fresh wake-mode advisory no longer prints on every gc command, and `gc hook` with no work explains itself on stderr. It inverts upstream's `TestAlwaysFreshWakeModeWarningIsNonFatalAndEmitted`. | none; carry-only | Upstream limits the advisory to config commands and explains an empty hook, so `cmd/gc/strict_warnings_test.go` needs no inversion. |
| Order-tracking sweep readers read Live (ga-v5vnyp) | e24b7d6b2 | `StaleOpenRuns` and `OrphanedOpenRuns` read the live store instead of the cache. Incident: the tracking watchdog was blind for 43h. | #5799 closed by us under the six-PR cap, not rejected | Upstream's two sweep readers read Live; `internal/orders/store_reads_test.go` passes on upstream. |
| `gc import check --verify-source` pin reachability | 3328a3190 | Checks that each pinned commit is reachable from a ref at its declared source. Upstream's import check has no such flag. | none; carry-only | Upstream's import check verifies pin reachability; `cmd/gc/cmd_import_verify_source_test.go` passes on upstream. |
| An unappliable patch is skipped and reported, not fatal (ga-djvbvp) | 093e36115, 73335023b | A `[[patches.*]]` entry that matches nothing is skipped with a warning instead of failing config load, including deferred implicit-agent patches. Incident 2026-08-25: one dangling agent patch stopped every gc command city-wide. | #5806 withdrawn 2026-09-01; carry-only | Upstream's `ApplyPatches` skips and reports an unappliable patch; `cmd/gc/patch_unapplied_warning_test.go` passes on upstream. |
| gc order history shows outcome and failure excerpt (ga-kahru9, ga-swawpo) | 49fb8b396, fb50e8e37 | `gc order history`, as a table and JSON, shows each run's outcome, and a failed run stamps an error excerpt on its tracking bead. Upstream #5499 puts the excerpt on the `order.failed` event only. | #5803 closed by us under the six-PR cap; #5499 covers the event | Upstream's order history renders outcome and error; the outcome cases in `cmd/gc/cmd_order_test.go` pass on upstream. |
| Order LATE-fire signal; a condition's exit 1 is told apart from a probe error (ga-44iyd) | ab4a0e5a5 | A fire behind its cadence logs a durable LATE line, and only a condition's exit 1 reads as not met, so a broken probe (exit 127, a missing binary) is reported apart from a deliberately false condition. Re-sync: merged with upstream #5774, whose cancel and signal guards stay; upstream reads every clean non-zero exit as not met. | #5774 covers the not-met wording and the cancel and signal guards; the exit 2+ probe-error split and the LATE line are carry-only | Upstream records late fires and reads exit 2 or above as a probe error; `TestCheckTriggerConditionExitAbove1IsProbeError` and the LATE cases in `internal/orders/triggers_test.go` pass on upstream. |
| Env-read golden and generated testenv imports for other rows | f5e249420 | No behavior of its own: the testenv import tests for `internal/liveness` and `internal/prguard`, and the env-read golden lines those rows add. | n/a | Goes with its parent rows (session liveness, PR close guard). |
| bd-1.0.5 wisps-tier probe for assigned open work (#5792) | 3cd93127c | On bd-1.0.5-compat cities the assigned-ready tier also probes open ephemeral rows, so an assigned open molecule-typed wisp root is served. It applies only with `bd_compatibility = "bd-1.0.5"`, which the alex city does not set. | #5792 withdrawn (the pin moved to 1.3.0) | rc.2 `bd ready --include-ephemeral --assignee=<id> --json` returns an open assigned patrol wisp root, or no fleet city uses bd-1.0.5 mode; then take upstream's work query and goldens. |
| mol-scoped-work teardown commits and pushes before removing (ga-6xehc5, ga-8as9s3) | d918a8318, 9664c75a8 (fork #72: a sediment-only tree is not committed or pushed), 6bb8933cc (fork #72: sediment classified by what gc materialized; tracked .claude/.beads edits secured), a2f49a077 (fork #72: only manifest-recorded symlinks and city-identical copies count as sediment), 3f277f7e0 (fork #72: behaviour test of the rendered secure step under bash and zsh) | The cleanup step commits WIP and pushes HEAD, or `refs/rescue/<bead>`, before removing a worktree, and stops if it cannot. Only what gc provably put in the tree is left out of the WIP commit: skill symlinks recorded in the sink's `.gc-skill-ownership.json`, that manifest, regular files under a skills sink byte-identical to the same path under `$GC_CITY_PATH` (city-synced copies such as search-sessions), `AGENTS-gc.md` and `.worktree-stale`. A sediment-only tree is neither committed nor pushed. Tracked edits under `.claude/` and `.beads/`, authored files and authored symlinks are work and are secured. Incident: a peer city lost 423 uncommitted lines, including a DB migration, twice in one night. Open (counsel-astra at fleet/2026-09-17.2): other providers' skill sinks and work-dir hook files (`.omp/hooks/gc-hook.ts`, `.codex/hooks.json`, ...) are not classified, and a recorded skill name's link target is not compared with the manifest. | none; carry-only (#5377 refuses non-linked worktrees only) | Upstream's `mol-scoped-work` secures work before removal, or a city overrides the formula through a pack layer. |
| Ephemeral in-progress probe pushes the assignee to bd (ga-2s6k) | folded into 6b94f85df | On bd-1.0.5-compat cities the AssignedInProgress probe asks bd for the identity's rows instead of scanning the ephemeral tier. Upstream #5757 memoizes one scan per Work query, which the re-sync accepts, so the pushdown is not extended; open: it filters holds and blockers after `--limit=20`, so a 21st usable row can be missed (follow-up bead). | #5757 covers the combined Work query | No fleet city sets `bd_compatibility = "bd-1.0.5"`, or upstream bounds the in-progress probe server-side. |
| hold:* prefix park excluded from pool demand (ga-uica16) | 6b94f85df, da95c336c | Any `hold:*` label parks a bead out of pool routing and demand, not only upstream's canonical `hold:mayor` and `hold:external`. Open: `demandRowServable` lacks the prefix rule, so the agreement corpus cannot hold a non-canonical hold row (Q6). | #5114 covers the canonical holds | Bridge wait-class parks move to `hold:external` with the class in a separate label (no Go), or upstream adopts a prefix rule. |
| Silent-work detector and publish-pointer bd gate (ga-krso22, ga-mmvpq1, ga-vh6cbz) | 9e1e719b3 | A core-pack order finds published work waiting on nobody (a bead arm that comments, an orphan arm with latched mail), and `gc bd` refuses the published state without a fetchable PR pointer. Upstream's core pack has no such detector. | none; carry-only (the order and script could ship from a qc-bridge pack) | Upstream's core pack ships an equivalent detector with both arms, or the order moves to a qc-bridge pack and only the bd gate stays. |
| Retention watchdog candidate and phantom counts (ga-28co77, ga-hujj6s) | folded into f45ee6255 | The bounded retention sweep logs how many candidates it received and how many deletes hit rows that no longer exist, beside #5802's stranded count. Diagnostic for the open ga-hujj6s question. | none; diagnostic | ga-hujj6s is resolved. |
| Order dispatch budget exhaustion log (ga-44iyd) | f45ee6255 | When the per-tick dispatch budget runs out with orders unvisited, the dispatcher logs it. Upstream #4491 already ships `max_dispatches_per_tick`; the log is re-expressed in upstream's fire loop. | #4491 absorbed the knob; the log is carry-only | Upstream logs budget exhaustion; the exhaustion-log case in `cmd/gc/order_dispatch_test.go` passes on upstream. |
| Order-tracking retention deferred off the startup pass (upstream PR #6433) | 322deb609 (replayed: red test, the city must report ready while the retention read blocks), e246ef814 (replayed: the boot deferral, Fixes #6429), cc3649d95 (replayed: the readiness test also waits for the deferred prune), afb3b98d4 (replayed: the test waits on the prune's deletes, no fixed sleep), 1f5ef92e1 (replayed: comments say the deferral moves the pass, not shortens it) | The startup-orders pass skips the order-tracking retention watchdog without stamping it, so the city reports ready before the unbounded closed-tracking list runs; the first steady-state tick then runs it inline. Boot-only: the steady-state tick still pays the pass (#2604's shape). Incident: westeros stayed unready for about 6 minutes after a restart with 73k closed tracking beads (gc-2ris). | PR #6433 (open), Fixes #6429 | #6433 merges; drop these five commits by content, because a squash merge changes their patch-ids. |

### Build, CI, deploy, tests, tmux (21)

| Behavior | Commits | What / why | Upstream | Drop when |
|---|---|---|---|---|
| proctable.KillByPID refuses to signal a tmux server (hq-nie0) | 7372c9e3f (replayed) | `KillByPID` refuses a target that is tmux infrastructure, classified with the scanner's own predicate, as a second layer behind #5392's scan fix. Incident hq-nie0: a misclassified tmux server was signalled and took a city down. | PR #6174 (open); replayed from its head 6b68a635f | #6174 merges; the replayed commit drops by patch-id. |
| Rebase-reconciliation and test-adapt fixups | 26c613fd1 (dashboard client and dist regenerated after re-sync #2's resolutions); part folded into 1f1685e06, 08a5f21d8, 61a7b3bbb, f4e461812, 486ea44bd, eee1e7bd8, 54644f169, df096d85f, d26fad0d4, 77304650c, 93de89d4c, 73335023b, 87c983b38, 3e40ab5dc | No behavior of its own: API adaptations, restored safety checks (fail-closed git probes, canonical worker_dir) and refreshed guard baselines for other rows, folded into those rows' commits. | n/a | Goes with the rows it adapts. |
| gc status runtime-probe budget of 2s (ga-63pjp) | e2c7fbd89 | The bounded runtime status probe waits 2s instead of 50ms. Since #5027, supervisor-managed cities route `gc status` through the API, so this may serve only the no-supervisor fallback (unsettled). | none; unsettled | `GC_DEBUG=1 gc status` on a supervisor-managed city, run with an upstream-built binary, routes through the API and prints no runtime-probe timeout. |
| Interactive tmux agents default to FORCE_COLOR=3 (ga-od2) | 705ed09bc | Interactive tmux sessions get `FORCE_COLOR=3` unless `FORCE_COLOR` or `NO_COLOR` is set, and agents with a suppressed startup prompt are skipped. Config env can set the value, but not that scope. | none; carry-only | Upstream ships interactive color handling, or the fleet sets `FORCE_COLOR` through provider env and accepts the wider scope. |
| make install swaps by atomic rename, verifies unpiped, rotates backups (ga-pmeo1, ga-mkbux) | 00216e5dc, bf67df612 | `make build` signs before the rename, `make install` runs the installed binary without a pipe and rolls back on failure, backups are pruned to the newest two, and `check-atomic-binary-swap.sh` guards the pattern. The class fix for the ga-l8pur outages on source builds; fleet machines install release assets. | none; carry-only | Upstream's install verifies and rolls back, or no one source-builds a deployed gc and the owners retire it. |
| launchd refresh re-registers the supervisor, verifies the new build, rolls back (ga-l8pur, ga-4v3ckk) | 990b69a3f, c778aca4d | `gc supervisor install` and the drift restart boot out, enable, bootstrap and `kickstart -p` the job, poll until the new build serves, and roll back otherwise; enable precedes bootstrap because upstream #5334 disables the job on stop (R4a). Upstream reuses the registration, whose cached launch constraints SIGKILL a swapped binary: a ~4h city outage on 2026-08-03, and again on 2026-08-17 (ga-l8pur). | #5334 made stop durable; issue-first candidate | Upstream's install and drift restart re-register launchd and verify the serving build; the refresh cases in `cmd/gc/cmd_supervisor_test.go` and `cmd/gc/drift_test.go` pass on upstream. |
| Test-city tmux server reaping on darwin (ga-utvl) | a6875ff6f, 712444ede | cmd/gc's test teardown kills the tmux servers under each socket root before deleting it, so a macOS run leaves no servers behind. Re-expressed on upstream's reaper (`tmuxtest.KillTmuxServersUnder`, SIGKILL escalation); the carry's keep-dir branch is gone (Q9). | #5215 covers Linux; #6296 and #6303 the crashed-run sweep; issue-first candidate | Upstream's end-of-run leak guard runs on darwin (`cmd/gc/tmux_leak_guard_other_test.go` is no longer a passthrough). |
| Clean-tree ignores for gc-injected dotted skills | d4c2d3336 | `.gitignore` ignores `/.claude/skills/*.*` and `/.claude/skills/.gc-skill-ownership.json`, which gc materializes into every seat worktree, so a local build there does not stamp `-dirty` and read as behind under the bounded-currency check. The Makefile half is upstream #4805. | #4805 absorbed the Makefile half; issue after the re-sync (precedent #5776) | Upstream's `.gitignore` ignores gc-injected skill files. |
| Bundled-pack pin guard for shipped example cities (ga-ylufqn) | 8c38b891e | Test only after Q4: `examples/bundled_pack_pin_test.go` fails when a shipped example pins a bundled pack at anything but `BundledPackImportVersion`. The example pins themselves are upstream's. | none; upstreamable guard | Upstream carries an equivalent guard, or by owner choice (the carry holds no pin delta). |
| golangci-lint 2.12 debt clearances (ga-tn4txq) | folded into d26fad0d4, 41d8a0e9a, fbb5702b8, 77304650c, 93de89d4c, 73335023b, 87c983b38, 63d0adbbd, da95c336c, eafc6512b, 0732398ef | No behavior of its own: lint and format fixes to carry code, folded into the commits that introduced each finding. | n/a | Goes with the rows it cleans. |
| Test flake: exec readiness marker written by the foreground child (ga-6y88yt) | a631ac9ac | Test only: closes a fork and SIGINT race measured at 14 failures in 40. | none; upstreamable test fix | Upstream's `TestProvider_StartCancellationInterruptsForegroundChild` writes the marker from the child. |
| Test flake: bounded localhost-SSH probe (ga-hz84hj) | c8a02e56e | Test only: the ssh availability probe and exec run under test-owned deadlines, so a pre-auth stall cannot burn the package's 10-minute alarm. | none; upstreamable test fix | Upstream bounds the probe in `internal/runtime/ssh/ssh_test.go`. |
| Deploy-freeze marker gates gc supervisor install and make install (ga-rfdkxp) | 35c585a31 | `~/.gc/deploy-freeze.json` blocks `gc supervisor install` and `make install` unless the freeze is acknowledged, so a routine deploy cannot land during a freeze. Upstream has no freeze. | none; deployment governance | The fleet roll recipe reads the freeze by hand everywhere, as a release consume already does, and the owners retire the marker. |
| make check-lean-local (ga-82cffd) | f7ba96113 | A fail-closed lean Go check over changed packages, for local and refinery gates. | none; carry-only | No gate calls `make check-lean-local`. |
| CI drops unused third-party apt sources before apt-get update | d8d206fd5 | Removes the runner image's google-chrome and microsoft-prod sources before each `apt-get update`, after a Hash Sum mismatch flake on dl.google.com (2026-09-09). Nobody has checked whether the flake recurs. | none; carry-only | A month of upstream CI shows no apt Hash Sum mismatch, or upstream drops those sources. |
| Staged gc binary signing with a stable identity (ga-f9dnub) | 59375eef2 | `scripts/sign-staged.sh` signs the staged binary with the box's wizard identity, so one TCC grant survives rebuilds; it is inert until an identity exists. The reviewers found upstream's `sign-darwin-local.sh` does the same when `GC_SIGN_IDENTITY` is set. | upstream `sign-darwin-local.sh` covers it with `GC_SIGN_IDENTITY` | Each laptop that adopted an identity sets `GC_SIGN_IDENTITY` in its window (qc-bridge fleet-gc-binary-roll section 5). |
| Fleet release workflow (gc-970o) | 09308a255, 13ac171ff (module warm that keeps the unit's checkout clean; was fork PR #62) | `.github/workflows/fleet-release.yml` builds a `fleet/*` tag for linux-amd64 and darwin-arm64 with `-tags gms_pure_go` and publishes the release that is the unit's attestation; a fork-only release lane. The checkout-clean module warm (fork PR #62) is folded in: a tag-push build must not stamp `-dirty`. | none; fork-only | The fleet stops cutting its own units. |
| Test binaries scrub ambient provider credentials at init (ga-fhbnmz leg B) | c42eba59e | A test binary drops live provider keys from its environment at init (opt out with `GC_ALLOW_AMBIENT_PROVIDER_CREDS_IN_TESTS`), so tests cannot spend money or forward keys into the gc processes they spawn. go-test tmux servers once outlived runs with a live key in their argv. | none; upstreamable | Upstream's testenv scrubs provider credentials; `internal/testenv/ambient_credentials_test.go` passes on upstream. |
| Tmux server-founding clients carry no secrets (ga-fhbnmz leg A) | 5700e91b1 | gc scrubs secret-named variables from the environment of any tmux command that can found a server on a named socket, so a cold-started server cannot inherit them. Re-sync: upstream #5425 already keeps secrets out of server argv, and this founding-client scrub replaced the carry's anchor session and `-N`. Re-sync #2: census baselines are upstream's plus this row's own delta (audit subprocess 693/201, audit fixed_sleep 493/175, debt subprocess 473/136, debt fixed_sleep 330/123, small subprocess 464/130, small fixed_sleep 330/123). | #5425 covers argv; the environment scrub is carry-only (outside upstream's threat model) | Upstream starts cold servers with a scrubbed environment; `TestRunCtxScrubsSecretEnvForServerFoundingClients` passes on upstream. |
| CI warms and retries Go module downloads once per job (ga-azybk8) | 00bc80218 | A `go-mod-warm` action downloads the module graph with three attempts and backoff after each setup-go, so shards stop failing on sum.golang.org HTTP/2 errors. Upstream's three new setup-go jobs are not warmed this window (CI speed, not correctness). | none; carry-only | The flake does not recur on upstream's tagged beads module graph over a month of CI, or upstream adds a retry. |
| Product-metrics testhook budget fits its own compile (gc-1c2b) | e6d3186a0 (the testhook budget, raised on its own timing artifact) | `cmd-gc-productmetrics-testhook` gets 12 minutes instead of 5. Its six tests take 0.87 seconds in total; everything else in the job is `go build` of cmd/gc under the `productmetrics_testhook` tag, on the 4-CPU runner `runner-policy` gives every login but the named maintainers. The job has run at 92-95% of the old budget on upstream's own main, and went over on the first branch whose `go.sum` moved, because that changes setup-go's cache key and the build is then cold. | none yet; filed upstream as the real fix — a warm build cache for the job, or a runner matching the budget | Upstream warms that job's build cache or gives it a runner its budget fits, and the job's own timing artifact shows compile well inside 5 minutes on a 4-CPU runner. |

## Not carried at the 2026-09-17 re-sync

One row per behavior the stack does not carry. Every row was dropped at the 2026-09-15 re-sync or
at its 09-16 rebase; the 2026-09-17 re-sync dropped none. "Dropped at integration" rows were decided
while resolving the 2026-09-15 merge. The original carry SHAs below are from the pre-09-10 lineage and resolve under
`fleet/2026-09-10` (`7b7ce4db2`) and, since `fleet/2026-09-10` is an ancestor of `archive/carry-operational-pre-20260915`, under that tag too; not under the live carry. 75 of 75 measured (woodhouse, 2026-09-15); 76 of 76 re-measured on 2026-09-17.

### Session liveness and beads cache (1)

| Behavior | Verdict | Evidence | Original carry SHAs |
|---|---|---|---|
| gc bd heartbeat writes gc.last_heartbeat_at to the liveness table (ga-lys454) | OBSOLETE | Upstream #5213 (6a4901269) forwards `gc bd heartbeat` to bd's native lease heartbeat, which writes no Dolt commit, and deleted the metadata key. | 6e3a1cd24, a4bd25d48, d653457d0 |

### Notifications, nudges, mail, prompts, providers (2)

| Behavior | Verdict | Evidence | Original carry SHAs |
|---|---|---|---|
| Claude hook-stdin session ids accepted as the resume key (ga-lqk3) | ABSORBED | Upstream #3954 (97bdcbe17) accepts claude hook-stdin session ids at the persist gate, and #5396 mints the claude session id up front. | 01ac00df4 |
| Nudge dead letters pruned after backing beads are reaped (#5328) | ABSORBED | A pure pick of upstream #5328 (9a1801719), already on upstream main. | 57006a8da |

### Reconciler, sessions, pools, reapers (4)

| Behavior | Verdict | Evidence | Original carry SHAs |
|---|---|---|---|
| Hook claim writes the alias-first assignee (ga-i44k, claim half) | ABSORBED | Upstream #4981 (2e1a9cf76, ours) made the same reorder, and #5663 refined it into `hookClaimAssigneeIdentity`, alias first. | 062ab7220 |
| Formula scope honors GC_RIG (ga-96qs) | ABSORBED | Upstream #4760 (9a88d149c) and #4848 honor `GC_RIG` in formula scope; the carry held only an unused helper and duplicate tests. | 7029c9314 |
| BEADS_ACTOR is the alias-first claim identity (we-m34w5, env half) | ABSORBED | Upstream #4981 (2e1a9cf76, ours) sets `GC_AGENT` and `BEADS_ACTOR` from `AssigneeIdentifier(info)`, pinned by `internal/session/lifecycle_actor_test.go`. | 36356f259 |
| Worktree reaper Darwin liveness source (ga-bq84cj) | ABSORBED | Upstream #4852 (02126121a) falls back to a bounded, fail-closed lsof scan when /proc is unreadable, chosen at runtime. | 08ed4ae7c |

### Dolt packs, doctor, managed Dolt (5)

| Behavior | Verdict | Evidence | Original carry SHAs |
|---|---|---|---|
| mol-dog-backup per-database budget from the order timeout (ga-1rmn3a) | OBSOLETE | The inversion came from the carry's own 1800s default; upstream #5329 bounds each sync attempt at 120s, three attempts, both overridable. Its `AwakeSessionBead.Alias` hunk moved to the ga-rht4v5 row. | 823078c57 |
| Dolt dog prompt names gc hook --claim (ga-tmzjx6) | ABSORBED | Upstream #5687 (d6e4339f7) rewrote the dog prompt around `gc hook --claim --drain-ack --json`, a superset of the carry text. | 3f4a87833 |
| Bundled core/bd pack pin names a carry commit | PIN | Upstream's `BundledPackImportVersion` (f895c0ff) stands; the ten example pin files were restored to upstream bytes at integration (Q4). westeros core still pins abcf2b639, which stays reachable through the archive tag. | 2ae8a3fb2, 1e38d8078, 6b747120d |
| Doctor order-firing-current honors suspended_on_start rigs (ga-axzhwv, fork PR #34) | ABSORBED | Upstream #5270 (a82b12651) decides rig suspension with `EffectiveRigSuspended`, a superset, under the same test name. | 0f37e0710 |
| dolt_transaction_commit set, then retired into a read-only tripwire (ga-7unsv0, ga-09xcry) | OBSOLETE | The net effect was a read-only check that `dolt_transaction_commit` is 0; upstream never sets it and Dolt defaults to 0. Only carry builds from 2026-08-26 to 2026-09-03 set it to 1. | 13fc83a4d, 95f7b4acc, 1b1547a59, d3285e8f4, 35b35a88d |

### Beads store, pins, Dolt endpoints (4)

| Behavior | Verdict | Evidence | Original carry SHAs |
|---|---|---|---|
| Beads module pin: go.mod, deps.env, dependency cap, beadsFleetPin test | PIN | Upstream #5830 (a95c730a5) pins beads v1.3.0-rc.2 in go.mod and deps.env, and `TestBDVersionPins` replaces the carry-only `beadsFleetPin` test. | 74407adde, e3c8ee5a0, 39db1cea5, 367ff5f2e, b0489cbfd, 09ea1b842, 0a5eebc9f, 435fa0000 |
| bd version-label compat in checkVersionCompat | PIN | Upstream #5184 passes an unconfirmable library, #5252 a newer same-major bd and #5830 two prereleases of one release; with go.mod on the rc.2 tag the carry's arms cannot fire. | ecad36bb3, 296312910, abf1ec89d |
| Shared deploy-branch CI greening: tmux manifest counts, stale CVE waivers | OBSOLETE | Its manifest counts covered carry-only tmux tests, and upstream's `.trivyignore.yaml` no longer lists gc for those CVEs. | b858c1f8e |
| Native Dolt serialization-conflict retries: Update (#5229), Close and Reopen (#5272) | ABSORBED | Pure picks of upstream #5229 (60b5c19f8) and #5272 (d7e1c3aea), both on upstream main. | f5bd61a37, 751ccdaf2 |

### Dispatch, orders, molecules, work queries (6)

| Behavior | Verdict | Evidence | Original carry SHAs |
|---|---|---|---|
| Claim work-query deadline raise (ga-kl6) | ABSORBED | Upstream #5289 (126029e5a) sets the work-query timeout to 150s and ties the claim window to it; carrying 90s would regress. | e9be5c902 |
| Duplicate-dispatch dedupe: ready-assignment adopt, same-formula convoy adoption (ga-d4rb, ga-1pql) | dropped at integration | Dropped whole at integration: upstream `gc hook --claim` promotes a ready assignment with a bd `--claim` readback, and upstream refuses a same-formula relaunch (#5432, #5420); combined with that refusal, the carried convoy adoption would close the live workflow's input convoy in the refusal's cleanup. Stack commits aaec125cb, 757e79643 sit where its commits were and carry other rows, by their own trailers. | 7eecacd36, 0849640b4 |
| Workflow delete strand guard (ga-ejwo1q) | ABSORBED | The row's own drop condition fired: #5802 merged upstream as 9700d9a48 (2026-09-15), a superset of the replayed head. At the 09-16 rebase the first replayed commit was skipped as absorbed and the second dropped by patch-id; workflowDeleteSkip, assertWorkflowDeleteLeavesNothingStranded and deleteWorkflowBeadUnguarded all resolve at 9700d9a48. | fd8e83c9d |
| Routed claim tier priority order (#5629) | ABSORBED | Upstream #5629 (48d68d13a) drops `--sort oldest` from the routed tier, checked by behavior and goldens. | 84581a85b, dfba620fa |
| Dispatch vars reach formula validation and instantiation (gc-kxyr) | ABSORBED | Upstream #5322 (7cf9c8b3f) and #5941 pass the resolved vars to validation and instantiation, on the dispatcher and in `gc order run`. | 98b27548c, f9461c025, d72767898 |
| Supervisor per-city loader applies daemon feature flags (#6236) | owner decision | A contract gap, not an incident cause: the startup reload applies the flags on a normal boot. Third-party PR #6269 covers the fix; the two `GC_SLING_TRACE` lines go with it. | d410d4207, 636894a4f, 01a7b5556, 8b2249922, af7f28ff7 |

### Build, CI, deploy, tests, tmux (9)

| Behavior | Verdict | Evidence | Original carry SHAs |
|---|---|---|---|
| tmux teardown refuses to signal a tmux server (ga-03ixvj) | OBSOLETE | Upstream #3985 (a74e4ca79) replaced the pid-string teardown with an identity-fenced kill plan that cannot include the server. | 51def01c8 |
| proctable scan never reports a tmux server as an agent root (hq-nie0) | ABSORBED | Upstream #5392 (a6b72d832) skips tmux infrastructure in the darwin and linux scans; the carry commit was its backport. | 762071902 |
| Provider-ledger waiver horizon: date bump, 14-day warning, lapsed report | ABSORBED | Upstream #5610 and #5652 give each waiver its own expiry with a 14-day warning and grace, audited nightly in strict mode. | 25b921dbf, 3345a9e7c, c70c91a7a, f98ede4ae |
| CI bd lockstep: CI's bd built from go.mod's beads pin (ga-yl326d) | PIN | Upstream #5830 pins rc.2 in go.mod and deps.env and CI installs that release archive; `TestBDVersionPins` ties them. | 2fee7e6bd, 8dbd02a3c, ca978d4a7, 36c4364ed |
| Temporary CI driver: ga-w1ollm ruleset-arming canary | NETZERO | The canary workflow was added, edited and deleted: net zero. Its ruleset record is in Branch governance below. | db1750e5c, 89183982f, b43a3c765, da79b5c76 |
| Temporary CI driver: ga-82cffd red-negative control | NETZERO | The red-negative control workflow was added, edited and deleted: net zero. | 372686fd2, bdc0d9b90, 8f8efcd2f, 0a624376d, 0284a67d0 |
| unit-core explicit per-package -timeout (ga-m00l03) | ABSORBED | Upstream #4803 sets `GO_TEST_TIMEOUT` (default 20m); a serial gate host sets 30m. | e29edae51 |
| CARRY.md ledger and deploy-recipe docs | LEDGER | CARRY.md only; this file replaces it, and the deploy recipe now lives in qc-bridge fleet-gc-binary-roll. | 68abb7b64, 4b68c77b7, 52aa3d774 |
| Generated reference docs (fork CI docs-freshness bot) | REGEN | Bot-regenerated docs/reference files; regenerated once from the final tree. | c6866befd, 9b35a3c61, 444343eed, 15a4dbd94, 7822fa965 |

## Counts

- 153 behaviors, one written decision each: 122 kept and 31 dropped (30 at the re-sync decision, 1 at integration); 122 + 31 = 153. The standing-hold row (fork PR #59) was added after the re-sync.
- Kept rows by source: 11 replayed from upstream PR branches; 102 re-expressed from the old carry, 4 of them carried only by later commits; 9 with no commits of their own (Cross-town mail send refusal names the hub leg (ga-5toacq); Mail reads wait 25s on the server and 30s on the client (ga-x49mfh); Named seats: close fails closed without config, and a closing session never detaches work claimed under its durable identity (ga-9n8hjv); Carry-era canonical bundled pins listed as superseded (gc-970o); Beads fleet pin: gc links the fleet beads build through a go.mod replace (gc-1c2b); Order-tracking retention deferred off the startup pass (upstream PR #6433); Product-metrics testhook budget fits its own compile (gc-1c2b); Rebase-reconciliation and test-adapt fixups; golangci-lint 2.12 debt clearances (ga-tn4txq)).
- Dropped at the re-sync decision, by verdict: ABSORBED 16, OBSOLETE 5, PIN 4, NETZERO 2, LEDGER 1, REGEN 1, owner decision 1.
- The stack above upstream `ad34febe7`: 21 replayed + 135 re-expressed + 1 integration commit (`16a0f96e9`, 96 files the resolution changed beyond the per-behavior replay) + 48 commits added after integration = 205 commits, ending at `874411024`; `git rev-list --count` over that range also counts the 2 earlier ledger commits (`a6cf5bed5`, `eb777d64a`), 207 in all.
- 6 of 141 re-expressed commits went empty at linearize; their hunks landed in later commits, named in their rows as "folded into" (M1 00134b38e, ee585f3f2, 0c953c4a3, 2a7d21147, 2244667fc, 0ed8d76d8).

## Beads pin

The native store is the beads library linked into `gc`, so `go.mod`'s beads requirement decides
the highest Dolt schema a `gc` binary can open. Since the 2026-09-15 re-sync (and still at the 2026-09-17 one), `go.mod` takes upstream
main's beads requirement unmodified, and adds one replace so the code gc links is the fleet
build of that same release (gc-1c2b).

| Where | Value |
|---|---|
| `go.mod` require | `github.com/steveyegge/beads v1.3.0-rc.2` (schema v66), upstream's line, unmodified |
| `go.mod` replace | `github.com/quickserve-ai/beads` at the fleet tag of that release — the same schema, plus the fleet's beads carries |
| `deps.env` | `BD_VERSION` and `BD_CURRENT_VERSION` are `v1.3.0-rc.2`; `BD_CURRENT_REF` is `c185735c38e2` |
| CI's `bd` | upstream's release archive at `BD_VERSION` everywhere except the contract bd-current cell, which builds from the module `go.mod` resolves — the replace target — so the bd under contract is the beads code gc links |
| Every machine's `bd` | moves with the fleet unit, as below |

`checkVersionCompat` (`internal/beads/contract/preflight_checker.go`) refuses the native store on a
confirmed mismatch between `bd`'s version and the beads version `gc` links. It passes an equal
version, a newer `bd` of the same major version, two prereleases of one release, and a linked
library it cannot confirm (a pseudo-version, a replaced module or a source build). The fleet rule
is stricter than the check: `bd`'s `main.Version` equals the beads version the unit links, so every
machine's `bd` moves with the unit.

The fleet beads fork (lyft; be-oau) is re-synced to rc.2 as the carry-v66 lineage, and its released
fleet tag is what the replace names. Because the linked library is then a REPLACED module,
`checkVersionCompat` reports it unconfirmable and passes for any `bd` with a readable context — the
label stops being a gate and stays a fleet rule, enforced by the unit's own preflight rather than by
gc. `make check-gomod-replace`, a CI step, still refuses a replace that names a pseudo-version, a
bare prerelease, a local path or a git ref; its one admission is the fork path at
`vX.Y.Z[-rc.N]-fleet.<YYYYMMDD>[.<serial>]`, with fixtures.

**Moving the pin moves the fleet.** A shared store's writers move in one window: a binary that
migrates a database past what another machine's `bd` knows produces schema skew from the other
side. On this carry, a store newer than the binary holds session reconciliation and reloads (the
schema-skew row) instead of falling back silently to the exec store.

**Schema slot 0054 (ga-grjijl).** Cherub's `hq` and `as` stores carried a forked migration 0054
(`0054_add_gc_route_index`, `.up.sql` sha256 `05085d4c…d66cc`) where upstream has
`0054_add_lease_columns` (`2e51058b…1680`), and `qcore`'s ledger recorded upstream's hash while
carrying both migrations' columns (measured 2026-08-21). The 2026-09-02 unfork window was to drop
the forked 0054. Whether every store now records upstream's 0054 and carries the lease columns is
unverified at the re-sync. Re-measure, read-only, before any schema window:

```sh
gc dolt sql -q "use hq; select version, content_hash from schema_migrations where version=54"
gc dolt sql -q "select count(*) from information_schema.columns where table_schema='hq' and table_name='issues' and column_name in ('lease_expires_at','heartbeat_at','row_lock')"
```

**A local beads checkout.** To build against a local beads tree, use an untracked `go.work`
(`go work init . /path/to/beads`). While it exists you are not building the fleet pin, and
`go build` does not say so; `go list -m github.com/steveyegge/beads` then reports the local
directory. The `go.work` ignore lines left with the dropped beads-pin row, so `git status` shows
the file: never commit it, and delete it before building anything to deploy.

## Deploy recipe

Fleet machines do not build `gc`. A unit is an annotated `fleet/YYYY-MM-DD` tag on
`carry/operational`. Pushing the tag runs `.github/workflows/fleet-release.yml`, which builds
linux-amd64 and darwin-arm64 from a fresh checkout with `-tags gms_pure_go` and publishes the
release that is the unit's attestation. Each machine installs the unit per qc-bridge
`shared/fleet-gc-binary-roll.v1.md`: the order, preconditions, swap, countersign and rollback live
there, not here.

Fork-specific notes that still hold:

- On macOS, `gc supervisor install` is the refresh step because of the launchd refresh row: it
  re-registers the job, polls until the new build serves, and rolls back otherwise. Upstream's
  install returns early when the plist is unchanged, so an upstream binary does not refresh after
  an in-place swap. Never refresh with a bare `launchctl kickstart -k` (ga-4v3ckk).
- On macOS, `gc start` refreshes, and so restarts, a live launchd supervisor. Both laptops already
  run this behavior.
- The deploy-freeze marker `~/.gc/deploy-freeze.json` blocks `gc supervisor install` and
  `make install`. A release consume runs no `make`, so the roll page's preconditions read the
  marker by hand.
- `make install` swaps by atomic rename, verifies the installed binary without a pipe, and keeps
  two backups. It serves development builds; a fleet machine installs the attested asset byte for
  byte.
- At each city's next bd/core pack re-pin, a city that relied on pushing every Dolt remote sets
  `GC_DOLT_REMOTE_<DB>=a,b` on the dolt-remotes-patrol order env (the gc dolt sync row). Without it,
  extra mirrors stop receiving pushes, and health flags them.
- The release workflow warms modules against a scratch modfile (formerly fork PR #62), so a
  tag-push build no longer stamps `-dirty`; still check the first unit for `-dirty` before
  countersigning.

## Rebase cadence

Overseer rule, 2026-09-15: `carry/operational` is re-synced onto upstream main at least every
seven days. The clock is the carry's merge base with upstream main. Once that base is seven days
old, everything else waits until the re-sync lands: no upstream PRs, fork picks, reviews or
investigations. This stack's base is 2026-09-17; the next re-sync is due by 2026-09-24.

Check the clock at every boot and before any fork or upstream work:

```sh
git fetch upstream && git fetch origin carry/operational
git log -1 --format=%cd --date=short "$(git merge-base upstream/main origin/carry/operational)"
```

A re-sync is all of these:

1. Refresh the fork's `main` from `upstream/main`.
2. Rebase `carry/operational` onto it.
3. Write a drop-or-carry decision for every ledger row.
4. Get `CI / required` green on the exact SHA.
5. Cut the fleet unit from the result.

A partner town's constraint is not a reason to wait: if one town's host cannot run the re-sync,
the other town runs it and the first reviews. Tell the other fork owner before touching the shared
carry.

The re-sync pipeline, in resync-tools (gc-iexd):

1. Classify: reviewer lanes place every carry commit into a behavior with a verdict and evidence,
   and `verdicts.py` merges them into `state/clusters.json`.
2. Fold: `fold2.py` folds the carry's history into one commit per behavior segment on the old base,
   tree-equal to the old tip.
3. M1: a rebase that drops the DROP segments (`seqedit.py`, `m1-plan.json`) leaves the kept stack on
   the old base. Every commit carries `Folded behavior:` and the original SHAs.
4. Merge: rows with an upstream PR are replayed from their PR branches onto upstream main, then M1
   is merged and resolved once per file. Owner decisions go to `decision-notes.md` and the
   overrides file.
5. Linearize: `linearize.py` re-expresses M1 as per-behavior commits on the new base, ending at the
   resolved tree, plus one integration commit.
6. Ledger: `gen_carry_md.py` writes this file from the decisions, the replay map, the row text and
   the linearize report.

## Branch governance (rulesets, measured 2026-09-15)

| Ruleset | Targets | Rules | Bypass |
|---|---|---|---|
| `carry-operational merge gate (ga-w1ollm)` (22632861) | `refs/heads/carry/operational` | changes by pull request (0 approvals); required status check `CI / required`, strict; no deletion; no non-fast-forward | RepositoryRole 5 (admin), mode always |
| `rig-ref-confinement (ga-w1ollm)` (22632844) | every branch except `refs/heads/polecat/**`, `refs/heads/pl/**` and `refs/heads/carry/operational` | blocks creation, update and deletion | RepositoryRole 5 (admin), mode always |

Both rulesets are active. Because the bypass mode is `always`, any push by a repository admin is a
bypass. The rig and automation identity is not bypass-capable: its writable branches are
`polecat/**` and `pl/**`, and `carry/operational` only by pull request. Never grant it bypass.

A weekly re-sync rewrites `carry/operational`, which the merge gate refuses as a non-fast-forward.
It lands by a declared admin-bypass `--force-with-lease` push, after `CI / required` is green on the
exact SHA through a draft pull request, with both towns told. The procedure lives in the
gas-city-carry skill (hub bead qc-51c35j.70).
